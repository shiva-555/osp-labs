apiVersion: apps/v1
kind: Deployment

metadata:
  name: notification-service
  namespace: dev

  labels:
    app: notification-service

  annotations:
    kubernetes.io/change-cause: "Initial Notification Service Deployment"

spec:
  replicas: 1

  selector:
    matchLabels:
      app: notification-service

  template:
    metadata:
      labels:
        app: notification-service

    spec:
      containers:
        - name: notification-service

          image: 027742774373.dkr.ecr.us-east-1.amazonaws.com/notification-service:latest
          imagePullPolicy: Always

          ports:
            - containerPort: 3504

          env:
            - name: host
              valueFrom:
                configMapKeyRef:
                  name: notification-service-config
                  key: DB_HOST

            - name: user
              valueFrom:
                secretKeyRef:
                  name: notification-service-secret
                  key: DB_USER

            - name: password
              valueFrom:
                secretKeyRef:
                  name: notification-service-secret
                  key: DB_PASSWORD

            - name: database
              valueFrom:
                configMapKeyRef:
                  name: notification-service-config
                  key: DB_NAME

            - name: PORT
              valueFrom:
                configMapKeyRef:
                  name: notification-service-config
                  key: PORT

            - name: NODE_ENV
              valueFrom:
                configMapKeyRef:
                  name: notification-service-config
                  key: NODE_ENV

          resources:
            requests:
              cpu: "100m"
              memory: "128Mi"
            limits:
              cpu: "250m"
              memory: "256Mi"

          readinessProbe:
            httpGet:
              path: /
              port: 3504
            initialDelaySeconds: 20
            periodSeconds: 10
            timeoutSeconds: 5
            failureThreshold: 3

          livenessProbe:
            httpGet:
              path: /
              port: 3504
            initialDelaySeconds: 60
            periodSeconds: 20
            timeoutSeconds: 5
            failureThreshold: 3