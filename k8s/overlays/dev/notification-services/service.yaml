apiVersion: v1
kind: Service

metadata:
  name: notification-service
  namespace: dev

spec:
  selector:
    app: notification-service

  ports:
    - name: http
      protocol: TCP
      port: 3504
      targetPort: 3504

  type: ClusterIP